{
  "name": "pageside",
  "version": "0.1.0",
  "description": "Minimal Chrome MV3 side-panel chat about the current page, for any OpenAI-compatible endpoint.",
  "private": true,
  "license": "MIT",
  "author": "Chad Carpenter",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chadcarpenter/pageside.git"
  },
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:js": "node --check extension/background.js && node --check extension/content.js && node --check extension/sidepanel.js && node --check extension/lib/redaction.mjs && node --check extension/lib/browser-context-protocol.mjs && node --check extension/lib/sse.mjs && node --check extension/lib/markdown.mjs",
    "verify": "npm run test && npm run check:js"
  },
  "engines": {
    "node": ">=20"
  }
}
